# Build trees (one per agent: build/<agent>)
build/

# Local dependency checkouts (pinned; see external/PINS.txt)
external/

# Tool caches / downloaded validators
scripts/.cache/
.cache/

# macOS noise
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.xcworkspace/
xcuserdata/
compile_commands.json

# CMake stray in-source artifacts (belt and braces — an in-source configure once polluted the
# tree; builds MUST use cmake -B build/<name>)
CMakeCache.txt
CMakeFiles/
Makefile
cmake_install.cmake
CTestTestfile.cmake
DartConfiguration.tcl
_deps/
FilterTableUS_artefacts/
vst3_helpers/
juce_binarydata_*/
*_include.cmake
